[workspace]
resolver = "2"
members = [
    "crates/*"
]

[workspace.dependencies]
# Define shared external dependency versions here
serde = { version = "1.0", features = ["derive"] }
bytemuck = { version = "1.18", features = ["derive"] }
log = "0.4"
